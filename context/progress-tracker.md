# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Design system foundation (`context/feature-specs/01-design-system.md`) — complete
- Editor chrome shell (`context/feature-specs/02-editor.md`) — complete
- Auth (`context/feature-specs/03-auth.md`) — complete
- Project dialogs (`context/feature-specs/04-project-dialog.md`) — complete
- Prisma data layer (`context/feature-specs/05-prisma.md`) — complete
- Project API routes (`context/feature-specs/06-project-apis.md`) — complete
- Wire editor home to real project API (`context/feature-specs/07-wire-editor-home.md`) — complete
- Editor workspace shell (`context/feature-specs/08-editor-workspace-shell.md`) — complete
- Share dialog (`context/feature-specs/09-share-dialog.md`) — complete
- Liveblocks setup (`context/feature-specs/10-liveblocks-setup.md`) — complete
- Base canvas (`context/feature-specs/11-base-canvas.md`) — complete
- Shape panel (`context/feature-specs/12-shape-panel.md`) — complete

## Current Goal

- Move to the next feature unit (see `context/feature-specs/` for what's next; none beyond `12-shape-panel.md` exist yet). The canvas can now have nodes added to it (drag a shape from the bottom toolbar, drop it on the canvas) and every node renders via a real custom `canvasNode` type, but still deliberately bare per `12`'s explicit scope limit: every shape (rectangle/diamond/circle/pill/cylinder/hexagon) renders identically as a plain bordered rectangle — no shape-specific outlines yet (diamond/hexagon/cylinder are supposed to eventually be inline SVGs per `ui-context.md`'s Canvas section). Also still open: no `Controls`, no custom edge rendering (`canvasEdge` type exists, nothing renders it differently from React Flow's default), no way to edit a node's label/color/shape after creation (labels are created empty and there's no UI to set them), no AI behavior. Likely next: node editing (label text, color picker from the 8 `NODE_COLORS`, shape swap), shape-specific SVG rendering, and/or the AI-generates-the-canvas flow the landing page's marketing copy already promises.

## Completed

- shadcn/ui installed and configured (`components.json`, style `radix-nova`, RSC, lucide icon library, `@/*` aliases).
- Added UI primitives: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea (`components/ui/*`, untouched post-generation).
- Installed `lucide-react`.
- Added `lib/utils.ts` with `cn()` (clsx + tailwind-merge).
- Wired the full dark theme token set from `context/ui-context.md` into `app/globals.css`, mapped both as app-level Tailwind utilities (`bg-base`, `text-copy-primary`, etc.) and onto the standard shadcn tokens (`--background`, `--primary`, `--card`, etc.) so `components/ui/*` renders the same palette.
- Forced dark mode by adding the `dark` class to `<html>` in `app/layout.tsx` (app is dark-only, no toggle).
- Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass; visually verified via a temporary headless-browser smoke test (removed after use) — dark background, cyan brand accent, working Button/Card/Dialog/Input/Tabs/Textarea/ScrollArea, no light-mode styling, no console errors.
- Editor chrome shell (`context/feature-specs/02-editor.md`):
  - `components/editor/editor-navbar.tsx` — fixed `h-14` top bar, left/center/right sections, sidebar toggle button swapping `PanelLeftOpen`/`PanelLeftClose` based on an `isSidebarOpen` prop, `bg-elevated` with bottom border. Center/right sections render as empty placeholders (spec only defines behavior for the left section and says the right stays empty "for now").
  - `components/editor/project-sidebar.tsx` — floating overlay (`fixed`, not in flow) that slides in/out via `translate-x` transform driven by an `isOpen` prop, positioned below the navbar. Header with title + close button, shadcn `Tabs` ("My projects" / "Shared") each with an empty placeholder state, full-width `New Project` button with a `Plus` icon at the bottom.
  - `components/editor/editor-dialog.tsx` — reusable dialog pattern wrapping `components/ui/dialog.tsx` primitives, restyled with app tokens (`bg-elevated`, `border-surface-border`, `rounded-3xl`). Takes `title`, optional `description`, `footer`, and `children` (body). No concrete dialog instances built yet, per spec.
  - Verified: `npx tsc --noEmit` and `npm run lint` pass; visually verified via a temporary route + headless-browser (Playwright + system Edge) smoke test (removed after use) — navbar renders with working toggle, sidebar slides in/out without pushing canvas content, tabs and empty states render, dialog opens centered with title/description/footer styled in the dark palette, no console errors.
- Auth (`context/feature-specs/03-auth.md`):
  - Installed `@clerk/ui` (peer of the already-installed `@clerk/nextjs`), needed for the `dark` base theme import.
  - `lib/clerk-appearance.ts` — shared Clerk `Appearance` config (`theme: dark` from `@clerk/ui/themes`, `variables` mapped entirely to the app's existing CSS custom properties via `var(--...)` — no hardcoded colors), passed to `ClerkProvider` so it cascades to `SignIn`, `SignUp`, and `UserButton` alike.
  - `app/layout.tsx` — wrapped in `ClerkProvider appearance={clerkAppearance}`.
  - `proxy.ts` (project root) — this Next.js version (16.2.10) renamed `middleware.ts` to `proxy.ts` (same underlying mechanism/type, `NextMiddleware`); uses `clerkMiddleware` + `createRouteMatcher` from `@clerk/nextjs/server`. Public routes are derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` (added to `.env.local` as `/sign-in`/`/sign-up` — they weren't set previously); everything else calls `auth.protect()`.
  - `app/page.tsx` — now an async Server Component: authenticated users `redirect("/editor")`, unauthenticated `redirect("/sign-in")` (belt-and-suspenders alongside the proxy-level redirect, per spec).
  - `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` — catch-all optional routes (required by Clerk's own internal step routing), each rendering `<SignIn />`/`<SignUp />` inside the shared `AuthLayout`.
  - `components/auth/auth-layout.tsx` — 50/50 two-panel layout (hidden below `lg`): left panel on `bg-surface` (vs. the right panel's `bg-base`) for contrast between halves, icon-badge logo (`Sparkles` in a `bg-accent-dim` square) + "BluePrint" wordmark at top, bold headline + subtext + a 3-item feature list (icon badge, title, description) in the middle, copyright line at the bottom; right panel centers the form only. No gradients, hero imagery, feature cards, or scroll-heavy layout.
  - `components/editor/editor-navbar.tsx` — added Clerk's `<UserButton />` to the navbar's right section (previously an empty placeholder).
  - Fixed a pre-existing bug in `app/globals.css`: `--font-sans` was self-referential (`--font-sans: var(--font-sans)`), so it never resolved to the loaded Geist Sans font and every page silently fell back to the browser's serif default. Now maps `--font-sans` → `var(--font-geist-sans)` and added the missing `--font-mono` → `var(--font-geist-mono)`, per `ui-context.md`'s typography table.
  - Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`proxy.ts` shows up as "ƒ Proxy (Middleware)" in the build output). Visually verified via a dev server + headless-browser (Playwright + system Edge) smoke test — unauthenticated visits to `/` and `/editor` redirect to `/sign-in?redirect_url=...`; `/sign-in` and `/sign-up` render the 50/50 dark layout with the cyan brand accent and Geist Sans applied correctly, no console errors.
- Project dialogs (`context/feature-specs/04-project-dialog.md`):
  - `lib/mock-projects.ts` — `Project` type, `slugify()` helper, and mock `INITIAL_MY_PROJECTS`/`SHARED_PROJECTS` arrays (no API/persistence, per spec).
  - `hooks/use-project-dialogs.ts` — the dedicated hook the spec calls for: tracks which dialog is open (`create` | `rename` | `delete`, with the target project for rename/delete), the name-input/derived-slug form state, and a loading flag; takes `onCreate`/`onRename`/`onDelete` callbacks and awaits a fake `setTimeout` delay before invoking them, to simulate async submission.
  - `components/editor/project-dialogs-provider.tsx` — `ProjectDialogsProvider` + `useProjectDialogsContext()`; owns the `myProjects` list as local component state (seeded from the mock data, mutated in place by the hook's callbacks — this is the "mock data only" persistence boundary, not a backend), and renders the three `EditorDialog`-based dialogs (Create with live slug preview, Rename prefilled + autofocused + Enter-to-submit, Delete destructive-confirm-only). Mounted in `app/editor/layout.tsx`, wrapping `EditorShell`, so both the sidebar and the editor home page can reach the same dialog/list state via context.
  - `app/editor/page.tsx` — now a client component: centered heading/description/`New Project` button (no card wrapper, per spec), wired to `openCreateDialog()`.
  - `components/editor/project-sidebar.tsx` — renders `myProjects`/`sharedProjects` from context; owned-project rows get Rename/Pencil and Delete/Trash2 icon buttons (visible on row hover/focus-within), shared-project rows render with no actions. Added a `lg:hidden` backdrop scrim (`bg-black/50`) below the navbar that closes the sidebar on click, for the mobile tap-outside-to-close behavior.
  - Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. Visually verified via a temporary public route (`app/smoke-test-04`, briefly added to `proxy.ts`'s public-route matcher to bypass Clerk, both reverted after) + headless-browser (Playwright + system Edge) smoke test — home screen content and `New Project` button render; Create dialog's slug preview updates live while typing (`My Cool Project!` → `/my-cool-project`) and the created project appears in the sidebar; Rename dialog opens prefilled and auto-focused, description shows the current name, Enter submits; Delete dialog has no input and a destructive-styled Confirm button, and removes the project on confirm; Shared tab renders `Q3 Roadmap` with zero action buttons; at a mobile viewport the scrim appears over the canvas and tapping it closes the sidebar. No console errors throughout.
- Prisma data layer (`context/feature-specs/05-prisma.md`):
  - `prisma/schema.prisma` — unchanged (generator `prisma-client` outputting to `../app/generated/prisma`, `datasource db { provider = "postgresql" }`, no `url`/`directUrl` — those live in `prisma.config.ts` per Prisma 7 convention); `prisma.config.ts` points `schema` at the `prisma/` directory (folder-based multi-file schema), confirmed working via `npx prisma validate`.
  - `prisma/models/project.prisma` (new file, multi-file schema) — `ProjectStatus` enum (`DRAFT`, `ARCHIVED`); `Project` model (`id` cuid, `ownerId` for the Clerk user id, `name`, optional `description`, `status` defaulting to `DRAFT`, optional `canvasJsonPath`, `createdAt`/`updatedAt`, separate `@@index([ownerId])` and `@@index([createdAt])`); `ProjectCollaborator` model (`id` cuid, `projectId` FK with `onDelete: Cascade`, `email`, `createdAt`, `@@unique([projectId, email])`, separate `@@index([email])` and compound `@@index([projectId, createdAt])`). No fields added beyond what the spec listed (plus the `id` primary keys every model needs).
  - `lib/prisma.ts` — cached singleton on `globalThis` (dev-only caching, per the standard Next.js hot-reload pattern). Branches in `createPrismaClient()`: `DATABASE_URL` starting with `prisma+postgres://` constructs `new PrismaClient({ accelerateUrl: databaseUrl })` (native Prisma 7 client option — no `@prisma/extension-accelerate` package needed for basic connectivity, only for cache-strategy features, which aren't in scope); otherwise builds `new PrismaPg({ connectionString: databaseUrl })` and passes it as `adapter`. This project's actual `DATABASE_URL` is a pooled `postgres://...pooled.db.prisma.io...` string (not `prisma+postgres://`), so it exercises the adapter-pg branch at runtime.
  - Ran `npx prisma migrate dev --name init` (created `prisma/migrations/20260708082842_init/migration.sql`) and `npx prisma generate` (client output to `app/generated/prisma`, already gitignored).
  - Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. Ran a temporary `tsx` script (removed after use) importing `lib/prisma.ts`'s singleton to confirm end-to-end: raw query succeeds, `project.create`/`projectCollaborator.create` succeed with defaults applied (`status: DRAFT`, timestamps set), and deleting the parent `Project` cascade-deletes its `ProjectCollaborator` rows.
- Project API routes (`context/feature-specs/06-project-apis.md`):
  - `app/api/projects/route.ts` — `GET` lists the authenticated user's own projects (`where: { ownerId: userId }`, newest first); `POST` creates a project owned by the authenticated user, defaulting a missing/blank `name` to `"Untitled Project"` and relying on the schema's default `cuid()` (no manually-generated IDs).
  - `app/api/projects/[projectId]/route.ts` — `PATCH` renames (requires a non-empty `name` in the body, 400 if missing); `DELETE` removes the project. Both fetch the project first (404 if it doesn't exist), then compare `project.ownerId` to the authenticated `userId` (403 if it doesn't match) before mutating.
  - Both files call `auth()` from `@clerk/nextjs/server` first and return `401` if `userId` is null — this is defense-in-depth alongside `proxy.ts`'s existing `auth.protect()`, which already blocks unauthenticated requests to every non-public route (including `/api/*`) at the middleware layer.
  - No UI wiring, per spec — `lib/mock-projects.ts` and `project-dialogs-provider.tsx` are untouched.
  - Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/api/projects` and `/api/projects/[projectId]` both show up as dynamic `ƒ` routes in the build output). Did not verify live `401`/`403` responses via direct HTTP against the dev server — Clerk's dev-instance keys require a browser-established `__clerk_db_jwt` handshake (set client-side by `clerk-js` on page load) before *any* request, authenticated or not, is evaluated past that handshake; curl requests without it get a `307` to `/sign-in` with reason `dev-browser-missing` regardless of method, before reaching the route handler or even Clerk's own signed-in/signed-out check. This is a curl-only testing artifact (real browser sessions already carry that cookie from normal page loads), consistent with why prior features behind `proxy.ts` were smoke-tested with a headless browser rather than curl. The 401/403/ownership logic itself was verified by code review against Clerk's documented `auth()` contract (already used successfully elsewhere in this app) and Prisma's generated types.
- Wire editor home to real project API (`context/feature-specs/07-wire-editor-home.md`):
  - `lib/projects.ts` (new) — `getOwnedProjects(ownerId)` / `getSharedProjects(email)`, thin Prisma helpers used server-side only; shared projects are resolved via `ProjectCollaborator.email` (there's no Clerk-user-id link for collaborators, only email, per the `05-prisma.md` schema).
  - `app/editor/layout.tsx` — now an async server component: calls `auth()` + `currentUser()`, fetches owned/shared projects via `lib/projects.ts`, and passes both arrays as props into `ProjectDialogsProvider`. This (not `app/editor/page.tsx`) is the actual fetch point, because the sidebar it feeds is mounted in the shared layout, not in the home page — the layout persists across `/editor` and the new `/editor/[projectId]` route, so fetching here keeps the sidebar populated on both. `app/editor/page.tsx` itself is unchanged (still a small client component using dialog context for its `New Project` button).
  - `hooks/use-project-actions.ts` (new, replaces the deleted `hooks/use-project-dialogs.ts`) — dialog/name/loading state as before, plus: a `roomId` preview (`slugify(name)` + a short random suffix regenerated each time the create dialog opens, cosmetic only — see next bullet); `submitCreate` posts to `/api/projects`, then `router.push`es to `/editor/${project.id}` (the id the server actually returned) and calls `router.refresh()`; `submitRename` PATCHes and calls `router.refresh()`; `submitDelete` DELETEs, and additionally `router.push("/editor")` when the deleted project's id matches the current route's `projectId` param (i.e. the user is viewing the workspace they just deleted), otherwise just refreshes.
  - Project ID / "room ID" alignment: `06-project-apis.md` requires the server to always auto-generate the Prisma `id` (no client-supplied IDs). So the room-id preview shown in the Create dialog (`slug-<suffix>`) is UI-only and is discarded after submit — the real navigation and the value that will eventually be used as the Liveblocks room id is the `id` the API returns. This was an explicit user decision (see conversation) to avoid reopening the "no custom IDs" rule from 06.
  - `components/editor/project-dialogs-provider.tsx` — no longer owns `myProjects` as local mutated state; now takes `myProjects`/`sharedProjects` as props straight from the server-fetched layout data and passes them through context unchanged. All mutations go through the API + `router.refresh()`/`router.push()` instead of local array splicing. Create dialog's slug preview text is now the room-id preview described above.
  - `components/editor/project-sidebar.tsx` — `Project` type now imported from `@/app/generated/prisma/client` instead of the deleted `lib/mock-projects.ts`; no behavioral changes.
  - `app/editor/[projectId]/page.tsx` (new) — minimal placeholder workspace route so `router.push` after create actually lands somewhere instead of 404ing. Server component: looks up the project, 404s (via `notFound()`) if it doesn't exist or the signed-in user is neither the owner nor a `ProjectCollaborator` by email, otherwise renders the project name and "Canvas coming soon." Building the real canvas (Liveblocks + React Flow) is out of scope here — deferred to a future feature spec per `project-overview.md`/`architecture-context.md`.
  - Deleted `lib/mock-projects.ts` and `hooks/use-project-dialogs.ts` (both already removed in the working tree before this session started; not recreated).
  - Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/editor/[projectId]` shows up as a dynamic `ƒ` route). Full end-to-end smoke test with a real signed-in session: created a Clerk dev-instance test user via the Backend API (`+clerk_test` email, deleted again after the test), signed in through the actual `/sign-in` UI with Playwright (system Edge) — including the "check your device" client-trust OTP step, both verified with the fixed dev-mode code `424242`. Then, as that real authenticated user: opened Create from the editor home, watched the room-id preview update live while typing, submitted, confirmed the browser navigated to `/editor/<real-cuid>` (not the cosmetic slug) and the placeholder workspace rendered the right project name; went back to `/editor` and confirmed the new project appeared in the sidebar (server-fetched, not mocked); renamed it via the sidebar action and confirmed the sidebar reflected the new name after `router.refresh()`; deleted it via the sidebar and confirmed it disappeared; created a second project, opened its workspace, deleted it from the sidebar while it was the active route, and confirmed the browser redirected to `/editor` (the "active workspace" interpretation of the spec's "archive workspace" wording, confirmed with the user beforehand). Cross-checked the database directly afterward (temporary `tsx` script, removed after use) and confirmed zero leftover `Project` rows for the test user — the full create/rename/delete cycle round-trips cleanly through Postgres. No console errors during the flow (one unrelated `pg` driver deprecation warning about `sslmode=prefer`, logged at server startup, not during the flow itself).

- Editor workspace shell (`context/feature-specs/08-editor-workspace-shell.md`):
  - `app/editor/[projectId]/` renamed to `app/editor/[roomId]/` (the spec names the route `/editor/[roomId]`) — this is the same Prisma project `id`/cuid as before, just a param rename; there is still no separate `roomId` column (see existing Architecture Decision below). `hooks/use-project-actions.ts`'s `useParams<{ projectId?: string }>()` and its `params?.projectId === target.id` delete-redirect check were updated to `roomId` to match. `app/api/projects/[projectId]/route.ts` (a different, unrelated dynamic segment) was left untouched.
  - `lib/project-access.ts` (new) — `getCurrentIdentity()` wraps `auth()` + `currentUser()` into `{ userId, email }` (Clerk primary email); `hasProjectAccess(project, identity)` returns true if `identity.userId` matches `project.ownerId` or `identity.email` matches any `project.collaborators[].email`. Both are plain exported functions outside any page component, per the spec's "Access helper exists outside the page component" check.
  - `components/editor/access-denied.tsx` (new) — centered layout, `Lock` icon in a `bg-subtle` circle, "You don't have access to this project." message, and a `Button asChild variant="outline"` wrapping a `Link` back to `/editor`.
  - `app/editor/[roomId]/page.tsx` — rewritten as a server component: redirects to `/sign-in` if `getCurrentIdentity()` has no `userId` (belt-and-suspenders alongside `proxy.ts`'s existing `auth.protect()`, same pattern as `app/page.tsx`); fetches the project by id (including `collaborators`); renders `AccessDenied` (not `notFound()`) for both a missing project and a project the user can't access via `hasProjectAccess`, matching the spec's "non-existent projects also show AccessDenied"; otherwise renders the existing canvas placeholder ("Canvas coming soon").
  - Workspace chrome (navbar title, Share/AI-toggle actions, right AI panel, sidebar highlight) lives in the already-shared `EditorShell`/`EditorNavbar`/`ProjectSidebar` rather than a second page-specific shell, to avoid double-mounting a navbar/sidebar on top of the ones `app/editor/layout.tsx` already renders for every `/editor/**` route:
    - `components/editor/editor-shell.tsx` — now reads `roomId` via `useParams<{ roomId?: string }>()` and looks up the matching project out of the `myProjects`/`sharedProjects` arrays already available from `useProjectDialogsContext()` (no extra fetch). Passes the resolved name to the navbar as `projectName`, passes `roomId` to `ProjectSidebar` as `activeProjectId`, and owns a new `isAiSidebarOpen` boolean state; the right AI placeholder panel (`hidden ... lg:flex`, `w-80`, "AI chat coming soon." with a `text-ai` `Sparkles` icon) only renders when both `roomId` and `isAiSidebarOpen` are true, and only on `lg`+ viewports.
    - `components/editor/editor-navbar.tsx` — new optional props `projectName`, `isAiSidebarOpen`, `onToggleAiSidebar`. Center section renders `projectName` when present (was always empty before). Right section conditionally renders a `Share2` icon button ("Share project" — visual only, no click handler, per the spec's "no sharing behavior yet") and a `PanelRightOpen`/`PanelRightClose` AI-toggle button (colored `text-ai` when open) whenever `onToggleAiSidebar` is passed, i.e. only on workspace routes; `/editor` home is unaffected since `EditorShell` only passes that callback when `roomId` is present.
    - `components/editor/project-sidebar.tsx` — new optional `activeProjectId` prop threaded through to both `ProjectList` renders (My projects and Shared); the row matching `activeProjectId` gets `bg-accent-dim text-brand` added to its existing classes.
  - Post-implementation fixes from user testing (same session): (1) sidebar rows weren't clickable — `ProjectList` rows in `project-sidebar.tsx` now take a required `onSelect` prop (`role="button"`, `tabIndex`, click + Enter/Space keyboard support) that `router.push`es to `/editor/${project.id}`; the Rename/Delete icon buttons call `event.stopPropagation()` in their `onClick` so clicking them opens their dialog instead of also navigating. (2) the AI panel was a normal flex child (`shrink-0 w-80`) so opening it visibly squeezed the canvas — `editor-shell.tsx`'s right `<aside>` is now `fixed top-14 right-0 bottom-0` with a `translate-x-full`/`translate-x-0` slide transition (same overlay pattern as the existing left `ProjectSidebar`), so it floats above the canvas instead of pushing it; `main` no longer changes width when the panel toggles.
  - Scope check: no Liveblocks, no real canvas, no working Share dialog/sharing logic — all placeholders, per spec.
  - Verified: `npx tsc --noEmit` (after clearing a stale `.next` type-validator cache that still referenced the old `[projectId]` route), `npm run lint`, and `npm run build` all pass (`/editor/[roomId]` is the new dynamic route in the build output). Full smoke test with two real Clerk dev-instance test users (`owner+clerk_test@…`, `stranger+clerk_test@…`, both created via the Backend API and deleted after) driven with Playwright (system Edge): unauthenticated request to `/editor/<anything>` redirects to `/sign-in`; signed in as owner, created a project, landed on `/editor/<roomId>`, and confirmed the navbar shows the project name, the Share and "Open AI panel" buttons render, clicking the AI toggle opens the right panel ("AI chat coming soon.", `Close AI panel` aria-label) and closing it removes the panel again; navigating to a nonexistent room id as the owner rendered `AccessDenied` with the back-to-`/editor` link; signed in separately as the stranger (no ownership/collaborator row) and confirmed visiting the owner's real room id also rendered `AccessDenied`. A follow-up check on the sidebar (viewing the project's own room, sidebar open) confirmed the active row's computed style actually resolves to the brand color (`bg-accent-dim`/`text-brand` → `rgba(0, 200, 212, 0.12)` background, `rgb(0, 200, 212)` text), not just that the text renders. No console errors beyond the pre-existing, unrelated `pg` `sslmode=prefer` deprecation warning logged at server startup. Test project and both test users were deleted afterward (verified via a temporary `tsx` script, removed after use). The two post-implementation fixes above were separately verified the same way (fresh Clerk test user + Playwright): clicking a sidebar row navigated to that room while clicking its Rename button opened the rename dialog without navigating; toggling the AI panel left the canvas `<main>` element's bounding-box width unchanged while the panel itself measured `position: fixed` and visually overlapped the canvas's right edge.

- Share dialog (`context/feature-specs/09-share-dialog.md`):
  - `lib/collaborators.ts` (new) — `enrichCollaborators(collaborators)` takes the raw `ProjectCollaborator[]` rows and, via `clerkClient().users.getUserList({ emailAddress: [...] })` (Clerk Backend API, case-insensitive match against any of a user's registered emails, not just primary), returns `{ id, email, name, avatarUrl }[]`; `name` comes from Clerk's `fullName` getter, `avatarUrl` from `imageUrl`; both are `null` when no Clerk user matches that email, and the UI falls back to showing the bare email — no local user table was added, per spec.
  - `app/api/projects/[projectId]/collaborators/route.ts` (new) — `GET` returns the enriched roster to anyone with access (`hasProjectAccess`: owner or existing collaborator via `lib/project-access.ts` from `08`), `401`/`404`/`403` in that order otherwise. `POST` is owner-only (`403` for non-owners, checked directly against `project.ownerId`, not `hasProjectAccess` — a collaborator must not invite); validates `email` with a simple regex, `400` if missing/invalid, `409` if a `ProjectCollaborator` already exists for that `(projectId, email)` pair (relies on the schema's existing `@@unique([projectId, email])`, no separate pre-check race), creates it, enriches just the one new row, returns `201`.
  - `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` (new) — `DELETE` is owner-only, `404` if the collaborator id doesn't exist or belongs to a different project than the one in the URL (path confusion guard), otherwise deletes and returns `204`.
  - `hooks/use-share-dialog.ts` (new) — holds collaborators/loading/invite-form/remove/copy state and exposes plain async functions (`load`, `submitInvite`, `removeCollaborator`, `copyLink`) with no `useEffect`. Deliberately *not* fetch-on-open via an effect: the new `react-hooks/set-state-in-effect` ESLint rule (part of `eslint-config-next`'s React Compiler-oriented rules) flags any direct `setState` call in an effect body, including the common "set loading, then fetch" pattern. Fixed by lifting the hook up into `EditorShell` and calling `shareDialog.load()` directly inside the same click handler that opens the dialog (`onOpenShareDialog`) — a real event handler, not an effect — rather than suppressing the rule.
  - `components/editor/share-dialog.tsx` (new) — presentational only, takes all state/handlers as props (owned by `EditorShell` via the hook, spread in with `{...shareDialog}`). Copy-link button swaps its label/icon to "Copied!"/`Check` for 2s (`setTimeout` in the hook). Owners get the invite form (email input + submit, inline error text) and a per-row remove (`X`) button; collaborators get neither — just the read-only roster (avatar-or-initial circle, name-or-email, secondary email line only when a name is known).
  - `components/editor/editor-navbar.tsx` — the existing (previously inert, from `08`) Share button now takes an `onOpenShareDialog` prop and calls it on click; still only rendered when `onToggleAiSidebar` is passed (i.e. only on workspace routes), unchanged gating.
  - `components/editor/editor-shell.tsx` — owns `isShareDialogOpen` state and the `useShareDialog` instance; derives `isOwner` client-side via Clerk's `useUser()` hook (`activeProject.ownerId === user.id`) — the same `activeProject` already resolved from `myProjects`/`sharedProjects` context for the navbar title/sidebar highlight from `08`. Renders `<ShareDialog>` only when `roomId && activeProject` are both present.
  - Fixed a pre-existing bug while building this (the user flagged black input text, which would have broken the new invite email field): `components/ui/input.tsx` and `components/ui/textarea.tsx` both carried a stray shadcn-default `text-base` class from generation. In this project `--color-base` (`#080809`, near-black, the page background) is defined as a `@theme` color token, which makes Tailwind v4 treat `text-base` as *that color* instead of its normal font-size meaning — a collision already flagged as a gotcha below, but not previously traced to an actual rendered bug. `text-copy-primary` (`#f0f0f4`) was already present alongside it in `input.tsx`, but `tailwind-merge` doesn't know these are custom-color conflicts (it still classifies `text-base` under the font-size group), so both classes survived into the DOM and CSS source order — not the class-attribute order — decided the winner, silently rendering near-black text. Removed `text-base` from both files (textarea had no color override at all, so it was equally broken); kept `text-sm` (dropped the now-meaningless `md:` prefix since `text-base` never usefully applied at any breakpoint here) and added `text-copy-primary` to `textarea.tsx`. This is an explicit exception to "don't modify `components/ui/*`" per `ai-workflow-rules.md`'s "only modify when a task explicitly requires it" — the user reported the bug directly.
  - Verified: `npx tsc --noEmit`, `npm run lint` (one pre-existing-pattern `@next/next/no-img-element` warning for the collaborator avatar `<img>`, not an error), and `npm run build` all pass (`/api/projects/[projectId]/collaborators` and its `[collaboratorId]` child both show up as dynamic `ƒ` routes). Full smoke test with two real Clerk dev-instance test users (`share-owner+clerk_test@…`, `share-collab+clerk_test@…` — the latter given a real first/last name specifically to verify Clerk enrichment, not just the email fallback — both created via the Backend API and deleted after) driven with Playwright (system Edge, two separate browser contexts for owner vs. collaborator sessions): confirmed the email input's computed text color is `rgb(240, 240, 244)` (matches `--text-primary`, confirming the black-text fix); owner opened Share from a freshly created project, invited an unregistered email (showed with an "N" initial-circle fallback, email-only), invited the real collaborator test user (showed enriched as "Chris Collaborator" with a real Clerk avatar `<img>`), got a `409`-backed inline error re-inviting the same email, saw "Copied!" feedback after the copy-link button, and removed the fallback collaborator (disappeared from the list). Separately signed in as the collaborator, confirmed they land on the room directly (no `AccessDenied` — access via the `ProjectCollaborator` row from `08`'s `hasProjectAccess`), and that their Share dialog shows the roster but has no email input and no remove buttons. Confirmed server-side enforcement directly (not just hidden UI) by having the collaborator's own authenticated session `fetch()` the invite endpoint from within the page — got `403`. No console errors throughout. All test data (2 Clerk users, 3 leftover `Project` rows from the run) cross-checked and deleted afterward via the Backend API and a temporary `tsx` script, respectively, both removed after use.
  - Visual redesign (same session, follow-up to a reference screenshot the user provided): reworked the dialog to a "Workspace link" bordered box (label + helper text + a `Copy link` button with a `Link` icon, replacing the earlier full-width plain button), a pill-shaped invite control (`Mail` icon + borderless `Input` + docked `Invite`/`Inviting…` button inside one bordered container instead of a separate icon-less input and button), and a "People with access" roster with a live `{n} total` count that now includes the **owner**, not just `ProjectCollaborator` rows — the owner gets a small `bg-accent-dim`/`text-brand` "OWNER" pill badge and no remove button.
    - `lib/collaborators.ts` — added `enrichOwner(ownerId)`, using `clerkClient().users.getUser(ownerId)` (direct Clerk-user-id lookup, not the email-list lookup `enrichCollaborators` uses, since we already know the owner's exact Clerk id from `project.ownerId`). `EnrichedCollaborator` gained a `role: "owner" | "collaborator"` field.
    - `GET /api/projects/[projectId]/collaborators` — response shape changed from a bare array to `{ owner, collaborators }`; `hooks/use-share-dialog.ts` updated to match (new `owner` state alongside `collaborators`).
    - Found and fixed a second instance of the same `text-base` color-token collision (see the `input.tsx`/`textarea.tsx` fix above) in `components/ui/dialog.tsx`'s `DialogTitle` — same root cause, this time discovered while redesigning the exact dialog that surfaces it: `DialogTitle`'s default classes had `text-base` with no color override at all, riding entirely on the token collision for its (broken) color, while `EditorDialog` separately passes `text-copy-primary`. Changed `text-base` → `text-lg` (keeps a title-appropriate size, drops the color meaning entirely so `text-copy-primary` is the only color utility in play). Same justification as before: directly blocked correct rendering of the feature being built, not a speculative drive-by fix.
    - Verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass. Visually verified against the user's reference screenshot with a fresh Clerk test user (`jsmastery-owner+clerk_test@…`, matching display name for a closer side-by-side comparison; deleted after) via Playwright screenshots at three states (email filled/pre-invite, post-invite, post-copy-link) — layout, spacing, the workspace-link box, the pill invite control, the OWNER badge, and the "Copied!" swap all matched the reference; confirmed `DialogTitle`'s computed color is `rgb(240, 240, 244)` (the `text-base` dialog-title fix). Test user and the one leftover `Project` row were deleted afterward.
  - Spacing pass (same session, user feedback that the dialog felt cramped): increased breathing room in the shared `EditorDialog` primitive itself, not just `ShareDialog`, so every dialog in the app benefits (Create/Rename/Delete project dialogs from `04` included) — `components/editor/editor-dialog.tsx`'s `DialogContent` override went from the shadcn default `p-4 gap-4` to `p-6 gap-6`, `DialogHeader` got an explicit `gap-1.5`, and `DialogFooter` (used by Create/Rename/Delete's Cancel/Confirm button rows, not by `ShareDialog` which has no footer) got matching `-mx-6 -mb-6 p-6 pt-0` so its edge-to-edge background aligns with the new padding. Within `share-dialog.tsx`: outer section gap `4`→`6`, workspace-link box padding `3`→`4`, invite pill vertical padding added (`py-2` on the wrapper instead of relying on a child `my-1.5`), roster header-to-list gap `1`→`3`, row gap `0.5`→`2`, row vertical padding `1.5`→`2`, avatar-to-text gap `2.5`→`3`, and small `mt-0.5`/`mt-1`/`mt-2` added between stacked label/helper-text and input/error-text pairs that previously had zero separation.
    - Verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass. Visually re-checked both the Create dialog and the Share dialog with a fresh Clerk test user (`spacing-check+clerk_test@…`, deleted after) via Playwright screenshots — both now have clearly more breathing room around the edges, between sections, and between list rows; the Create dialog's footer (Cancel/Create buttons) still sits flush with rounded bottom corners at the new padding. Test user and the one leftover `Project` row were deleted afterward.

- Liveblocks setup (`context/feature-specs/10-liveblocks-setup.md`):
  - `liveblocks.config.ts` (project root, was already present as a stock template from package install but not yet filled in) — `Presence` now `{ cursor: { x: number; y: number } | null; isThinking: boolean }`; `UserMeta` now `{ id: string; info: { name: string; avatar: string; color: string } }`. The still-unused placeholder types (`Storage`, `RoomEvent`, `ThreadMetadata`, `RoomInfo`) were changed from bare `{}` to `Record<string, never>` — `{}` as a type is an ESLint error (`@typescript-eslint/no-empty-object-type`, "allows any non-nullish value") that pre-existed in the untouched template and would have failed `npm run lint` once the file was no longer fully commented-out.
  - `lib/liveblocks.ts` (new) — `getLiveblocksClient()`, a `globalThis`-cached singleton for `@liveblocks/node`'s `Liveblocks` server client (same dev-hot-reload caching pattern as `lib/prisma.ts`), plus `cursorColorForUser(userId)`, a deterministic hash-to-index picker over a fixed 10-color palette (no randomness, same user always gets the same cursor color). Deliberately a lazily-called function rather than an eagerly-constructed module-level singleton (see the build-time gotcha below).
  - `app/api/liveblocks-auth/route.ts` (new) — `POST` only. Same 401/403 convention as the existing project APIs: `getCurrentIdentity()` first (`401` if signed out), then reads `room` (not `roomId`) from the JSON body — this is the literal field name `@liveblocks/client` sends to a string `authEndpoint` (confirmed by reading `@liveblocks/core`'s compiled source, `fetchAuthEndpoint(..., { room: options.roomId })`), `400` if missing. Looks up the `Project` by that id (including `collaborators`) and reuses `hasProjectAccess()` from `lib/project-access.ts` (from `08`) — `403` if the project doesn't exist or access is denied, per spec ("Return 403 for unauthorized project access"); a missing project intentionally also yields `403` rather than `404`, matching the same non-leaking convention `08`'s page already used for `AccessDenied`. On success: `getOrCreateRoom(roomId, { defaultAccesses: [] })` (private room — access is enforced entirely by our own check above, not Liveblocks group/default accesses), then `currentUser()` for `fullName`/`imageUrl` (falls back to the primary email, then `"Anonymous"`, if Clerk has no name on file), `cursorColorForUser(identity.userId)` for the color, `liveblocks.prepareSession(userId, { userInfo: {...} })` + `session.allow(roomId, ["*:write"])`, then returns `session.authorize()`'s `{ status, body }` directly as the HTTP response (its `body` is already the raw JSON text Liveblocks' `/v2/authorize-user` endpoint returned — this is the documented shape for a Next.js route handler passthrough).
  - `.env.local` — added an empty `LIVEBLOCKS_SECRET_KEY=` placeholder with a comment pointing at `https://liveblocks.io/dashboard/apikeys`. No real key was available in this session (user chose to add it themselves later); the route and `getLiveblocksClient()` are written to read it from the environment at call time, so nothing else needs to change once a real `sk_...` key is added.
  - No client-side wiring (`LiveblocksProvider`/`RoomProvider`, actually calling this auth endpoint from the browser) — out of scope per the spec, which only asked for the config, the cached client, and the auth route itself.
  - Verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass (`/api/liveblocks-auth` shows up as a dynamic `ƒ` route). Full smoke test with two real Clerk dev-instance test users (`lb-owner+clerk_test@…`, `lb-stranger+clerk_test@…`, both created via the Backend API and deleted after) driven with Playwright (system Edge, separate browser contexts per user): anonymous `GET /api/liveblocks-auth` redirected to `/sign-in` (existing `proxy.ts` `auth.protect()`, unchanged); owner signed in, created a project via the existing `/api/projects` POST, then called `/api/liveblocks-auth` with `{ room: <that project's id> }` — got a `500`, and the dev server log confirmed the exact throw site was `getLiveblocksClient` inside the route's `POST` handler (i.e., it passed the identity check, the `hasProjectAccess` check, and reached the real Liveblocks call, only failing because the placeholder `LIVEBLOCKS_SECRET_KEY` isn't a real `sk_...` key yet — the intended, narrow failure point given no key was available); same owner calling with a nonexistent room id got `403` (not 500), confirming the access check runs and short-circuits before ever touching Liveblocks; the stranger (no ownership/collaborator row) calling with the owner's real project id also got `403`. Both test users and the one leftover `Project` row were deleted afterward (the script itself, run from the `npx` playwright cache directory per the established gotcha, was also deleted after use). Did not verify an actual successful token issuance end-to-end, since that requires a real Liveblocks secret key that wasn't available in this session — the user was asked and chose to skip live token testing and add their own key later.

- Base canvas (`context/feature-specs/11-base-canvas.md`):
  - `types/canvas.ts` (new) — `CanvasNodeShape` (`"rectangle" | "ellipse" | "diamond"`), `CanvasNodeData` (`label`, `color`, `shape`, extending `Record<string, unknown>` per `@xyflow/react`'s `Node<NodeData, NodeType>` generic constraint), and the two type-discriminated aliases the spec asked for: `CanvasNode = Node<CanvasNodeData, "canvasNode">`, `CanvasEdge = Edge<Record<string, never>, "canvasEdge">` (edges carry no data yet, per the "don't add custom edge rendering yet" scope limit — the `"canvasEdge"` type string exists for a future renderer to key off of, nothing consumes it yet).
  - `components/editor/canvas.tsx` (new, client) — the actual `<ReactFlow>` instance. Calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ nodes: { initial: [] }, edges: { initial: [] }, suspense: true })` from `@liveblocks/react-flow` and passes its `nodes`/`edges`/`onNodesChange`/`onEdgesChange`/`onConnect` straight into `ReactFlow`. `connectionMode={ConnectionMode.Loose}` (loose connection behavior per spec), `colorMode="dark"` (app is dark-only), `fitView`, a `<Background variant={BackgroundVariant.Dots} />`, and a `<MiniMap />`. No `<Controls />` and no custom `nodeTypes`/`edgeTypes` registered, per the spec's scope limits — `CanvasNode`/`CanvasEdge` exist as shared types only, nothing renders them differently from React Flow's defaults yet. Imports `@xyflow/react/dist/style.css` directly in this file (Next.js App Router bundles CSS imported from any component; this is xyflow's own documented pattern, not project-specific).
  - `components/editor/canvas-room.tsx` (new, client) — `CanvasRoom({ roomId })`, mounted from the workspace page. Nests `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={roomId} initialPresence={{ cursor: null, isThinking: false }}` (the `liveblocks.config.ts` `Presence` type from `10` requires `isThinking`, even though the spec only mentions `cursor: null`; `false` is the innocuous default) → `CanvasConnectionGuard` → `CanvasErrorBoundary` → `ClientSideSuspense fallback={<CanvasLoading />}` → `Canvas`.
  - `components/editor/canvas-error-boundary.tsx` (new, client) — `CanvasErrorFallback` (presentational: "Couldn't connect to the canvas." + "Refresh the page to try again.") and `CanvasErrorBoundary`, a plain class-component error boundary (no `react-error-boundary` package is installed in this project, so this is hand-rolled rather than pulling in a new dependency for one component) that renders `CanvasErrorFallback` if a descendant throws during render — this is defense-in-depth for genuine synchronous render throws (e.g. a malformed `useLiveblocksFlow` suspense rejection), not the main mechanism for connection-error reporting (see below).
  - Architecture finding, verified against actual runtime behavior (not assumed from training data, per `AGENTS.md`): Liveblocks connection/auth failures do **not** throw into the React tree, so a plain `ErrorBoundary` around `ClientSideSuspense` never catches them — confirmed by smoke-testing with the (still-placeholder) `LIVEBLOCKS_SECRET_KEY`, which produces a real `500` from `/api/liveblocks-auth`: the canvas stayed stuck on the `ClientSideSuspense` loading fallback indefinitely instead of showing the error boundary's fallback. Reading `@liveblocks/core`'s compiled source confirmed why: on an auth-endpoint non-2xx response, `fetchAuthEndpoint` only throws `StopRetrying` (which reaches `useErrorListener` via `fireErrorEvent`) for a fixed `NON_RETRY_STATUS_CODES` list (`400, 401, 403, 404, 405, 410, ...`) — anything else (including `500`) throws a plain `Error`, which the client's internal state machine treats as transient and retries forever with backoff, only logging to the console. This is correct/intentional behavior on Liveblocks' part (a `500` could be a transient blip and is worth retrying), not a bug — but it means the spec's "error fallback for Liveblocks connection issues" needed `useErrorListener`, not just an `ErrorBoundary`, to actually fire for the realistic unrecoverable case (e.g. `403` from our own route's `hasProjectAccess` check). Added `CanvasConnectionGuard` (in `canvas-room.tsx`) — a small client component that calls `useErrorListener` and swaps to `CanvasErrorFallback` on any reported error — wrapping `CanvasErrorBoundary`/`ClientSideSuspense`. Verified directly: a temporary test route mounting `CanvasRoom` with a nonexistent `roomId` (which our own `/api/liveblocks-auth` correctly turns into a `403`, per `10`'s "missing project also yields 403" convention) rendered `CanvasErrorFallback` within a few seconds; the real placeholder-key `500` case continued to correctly show the loading state (retrying), not a false error. Temporary route was deleted after use, confirmed via `git status`.
  - `app/editor/[roomId]/page.tsx` — the only change to the existing workspace page: the "Canvas coming soon." placeholder `<div>` is replaced with `<CanvasRoom roomId={project.id} />`. Everything above it (identity check, `notFound`-via-`AccessDenied` access check) is untouched — the page stays a server component per spec item 1; `CanvasRoom` is the client boundary.
  - Verified: `npx tsc --noEmit`, `npm run lint` (zero new warnings; the 3 pre-existing `share-dialog.tsx`/`use-share-dialog.ts` warnings are unrelated), and `npm run build` all pass. Full smoke test with real Clerk dev-instance test users (Backend API + Playwright/system Edge, same pattern as prior features; all deleted after) — signed in, created a project through the existing UI, landed on `/editor/<roomId>`, confirmed the navbar/sidebar/chrome all render correctly around the canvas area and the canvas shows "Loading canvas…" (expected, since no real `LIVEBLOCKS_SECRET_KEY` is set — see the `10` session's same caveat) rather than crashing or rendering blank; confirmed via browser console that the client correctly attempts `POST /api/liveblocks-auth` with the room id. Separately verified the `CanvasConnectionGuard`/`useErrorListener` fallback path with a temporary test route (deleted after) hitting a real `403`. Did not verify actual live node/edge sync between two connected clients, since that requires a real Liveblocks secret key that isn't set in this environment (same limitation noted at the end of `10`'s entry) — the `useLiveblocksFlow` wiring itself was verified against `@liveblocks/react-flow`'s actual shipped `.d.ts` (not assumed from training data) to confirm the hook's return shape and options match what's passed to `ReactFlow`. One Playwright gotcha hit during this session's smoke testing, worth recording: Clerk's post-`client-trust` client-side redirect chain (`/sign-in/client-trust` → `/` → `/editor`) is flaky under Playwright automation in this dev environment — the page can get stuck showing a blank `/` indefinitely even though the server-side `redirect()` logic is correct and a direct `page.goto("/editor")` immediately after works fine (session cookie is already set by that point). Workaround: after completing the OTP/client-trust step, navigate directly to the target route with `page.goto()` instead of waiting for the client router to complete the hop on its own.

- Shape panel (`context/feature-specs/12-shape-panel.md`):
  - `types/canvas.ts` — `CanvasNodeShape` changed from the placeholder `"rectangle" | "ellipse" | "diamond"` (guessed during `11`, never exercised) to the actual 6-shape set from `ui-context.md`'s Canvas section: `"rectangle" | "diamond" | "circle" | "pill" | "cylinder" | "hexagon"`. Added the two constants `ui-context.md` explicitly says live here: `NODE_COLORS` (the 8 fill/text pairs from that doc, `NODE_COLORS[0]` = the documented default `#1F1F1F`/`#EDEDED`) and `NODE_SHAPES` (each shape's display label + a `defaultSize: { width, height }`, per spec item 3's sizing rules — rectangle wider-than-tall `160×80`, circle square `100×100`, diamond sized up to `180×180` since a diamond's inscribed area is much smaller than its bounding box for the same box size, pill/cylinder/hexagon given similarly reasonable proportions). Also added `DEFAULT_NODE_COLOR` (= `NODE_COLORS[0].fill`), `getNodeTextColor(fill)` (looks up the matching text color for a given fill, falls back to the default pair), `SHAPE_DRAG_MIME_TYPE` (a custom `"application/x-blueprint-shape"` DataTransfer MIME type), and `ShapeDragPayload` (`{ shape, width, height }`, the drag payload spec item 3 asks for).
  - `components/editor/shape-panel.tsx` (new) — the floating pill toolbar (`rounded-full`, `bg-elevated`, `border-surface-border`, absolutely positioned `bottom-6` and centered over the canvas). Renders one `draggable` icon button per `NODE_SHAPES` entry (Lucide `Square`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon`, all of which exist as real exports); `onDragStart` serializes a `ShapeDragPayload` as JSON onto `event.dataTransfer` under `SHAPE_DRAG_MIME_TYPE`.
  - `components/editor/canvas-node.tsx` (new) — the "basic renderer" spec item 7 asks for: every shape renders identically as a bordered `rounded-lg` div sized to the node's `width`/`height`, background = `data.color`, text = `getNodeTextColor(data.color)`, label centered and truncated. Also renders 4 `Handle`s (one per side, all `type="source"` since `connectionMode` is `Loose`) so nodes stay connectable now that a custom renderer replaces React Flow's default node (which draws its own handles automatically) — styled per `ui-context.md`'s "Connection Handles" section (small white circles, `opacity-0` by default, revealed via `group-hover:opacity-100`). Registered as `nodeTypes={{ canvasNode: CanvasNode }}` on `<ReactFlow>` in `canvas.tsx`.
  - `components/editor/canvas.tsx` — restructured into an outer `Canvas` that wraps an inner `CanvasInner` in `ReactFlowProvider`, because spec item 4/5 (`dragover`/`drop` handling, converting screen→canvas coordinates "using React Flow") needs `useReactFlow()`'s `screenToFlowPosition`, which only works in a component *inside* `<ReactFlow>`'s own provider tree — calling it in the same component that renders `<ReactFlow>` throws, so the hook had to move to a child. `CanvasInner` wraps `<ReactFlow>` + `<ShapePanel>` in a `relative` div carrying `onDragOver` (calls `preventDefault()` + sets `dropEffect = "copy"`, required for the browser to allow a drop at all) and `onDrop`: reads the `SHAPE_DRAG_MIME_TYPE` payload, calls `screenToFlowPosition({ x: event.clientX, y: event.clientY })` for the canvas-space center point, generates an id via `` `${shape}-${Date.now()}-${counter}` `` (a module-level `nodeIdCounter`, per spec item 6's "shape name, timestamp, and a counter"), builds a `CanvasNode` (empty label, `DEFAULT_NODE_COLOR`, the dragged shape, positioned so the drop point is the node's *center* rather than its top-left corner), and dispatches it via `onNodesChange([{ type: "add", item: newNode }])` — confirmed by reading `@liveblocks/react-flow`'s compiled source (`dist/lib/flow.js`) that its `onNodesChange` has its own `applyNodeChanges` handling `case "add"` exactly like React Flow's own reducer, so this goes through the same Liveblocks-Storage-backed mutation path as position/selection changes, no separate "addNode" call needed (the only such API, `mutateFlow`'s `flow.addNode()`, is server-only from `@liveblocks/react-flow/node`).
  - Verified: `npx tsc --noEmit`, `npm run lint` (zero new warnings), and `npm run build` all pass. Full functional smoke test (delegated to a subagent) with a real Clerk dev-instance test user and a real `LIVEBLOCKS_SECRET_KEY` (now set in `.env.local` — no longer a placeholder, see the `10`/`11` sessions' caveat about this being unset) via Playwright (system Edge): the canvas now fully connects (no longer stuck on "Loading canvas...", the expected result once a real key is present); the 6-icon toolbar renders at bottom-center with correct shape icons and `aria-label`s. Native HTML5 drag-and-drop (real `dataTransfer`-based `dragstart`/`dragover`/`drop`, not just mouse movement) was simulated via `page.evaluate()` constructing a real `DataTransfer` and dispatching synthetic `DragEvent`s — dropping a Rectangle produced a new `canvasNode` element with a 2:1 (wide) bounding box matching the `160×80` default scaled by canvas zoom, dropping a Circle produced a 1:1 (square) bounding box matching `100×100`; both rendered as bordered rectangles with empty centered labels and the default dark fill, per the "basic renderer" scope. Zero console errors throughout. Test user, all leftover `Project` rows (including one from an earlier failed script iteration), and all temporary scripts were deleted afterward; `git status` confirmed clean.

## In Progress

- None.

## Next Up

- No queued next feature spec beyond `12-shape-panel.md` — see "Current Goal" above for likely candidates (node editing UI, shape-specific SVG rendering, AI-generates-the-canvas).

## Open Questions

- None currently open.

## Architecture Decisions

- `context/ui-context.md`'s theme table only gave example Tailwind utility names (`bg-base`, `text-copy-primary`, etc.) without a full mapping for every token. Resolved by extending that table with an explicit "Tailwind Utility" column for every CSS variable, following the naming pattern implied by the given examples (e.g. `border-*` variables → `border-surface-border[-subtle]`, `text-*` variables → `text-copy-*`, state colors → `text-*`/`bg-*` pairs).
- App-level components use the custom tokens documented in `ui-context.md` (`bg-base`, `text-copy-primary`, ...). The shadcn tokens (`bg-primary`, `text-foreground`, `border-border`, ...) remain reserved for use inside `components/ui/*`, both point at the same dark palette so they render identically.
- This project's Next.js version (16.2.10) renamed the `middleware.ts` file convention to `proxy.ts` (functionally identical — same `NextMiddleware` type, same execution model, just a new file name and export name). `@clerk/nextjs`'s `clerkMiddleware()` helper still returns a `NextMiddleware`-compatible function, so it default-exports cleanly from `proxy.ts` with no adapter needed.
- Prisma 7 breaking changes from what training data assumes: connection URLs live in `prisma.config.ts`, not in the `datasource` block of `schema.prisma`; `PrismaClient` requires either `adapter` or `accelerateUrl` in its constructor (`new PrismaClient()` with no args throws); the generator is `provider = "prisma-client"` (not `prisma-client-js`) with a mandatory `output` path — it no longer generates into `node_modules`. Reference: `.agents/skills/prisma-upgrade-v7/` and sibling `prisma-*` skills (not yet wired into the `Skill` tool's registry, but readable directly as markdown under `.agents/skills/`).
- Multi-file Prisma schema (`prisma/schema.prisma` for generator/datasource, `prisma/models/*.prisma` for models) works out of the box in this project's Prisma version once `prisma.config.ts`'s `schema` field points at the `prisma/` directory rather than a single file — confirmed via `npx prisma validate`.
- The project's Liveblocks room id is just its Prisma `id` (the server-generated cuid) — there is no separate `roomId` column and none was added. `07-wire-editor-home.md` asked for the create hook to generate a slug+suffix "room id" client-side before calling the API, but `06-project-apis.md` forbids client-supplied ids ("use the schema's existing ID strategy"); resolved (user-confirmed) by treating that generated string as a cosmetic preview only in the Create dialog, and using the real `id` the API returns for both navigation and (whenever Liveblocks is actually wired up) the room id.
- Project-list data for the editor sidebar is fetched in `app/editor/layout.tsx`, not `app/editor/page.tsx`, even though `07-wire-editor-home.md` says "the editor home page is a server component." The sidebar is mounted by the shared layout (so it also needs to be populated for `/editor/[roomId]`, not just `/editor`), and `layout.tsx` was already a server component with no client-only content of its own — so it's the natural single fetch point. `ProjectDialogsProvider` no longer keeps its own mutated copy of the project list; it just forwards the layout's server-fetched props through context, and mutations rely on `router.refresh()`/`router.push()` to pull fresh data rather than local array splicing.
- `10-liveblocks-setup.md`'s auth route reads `room` (not `roomId`) from the POST body because that's the literal field name `@liveblocks/client` sends when `authEndpoint` is a plain string URL (verified by reading `@liveblocks/core`'s compiled source rather than assuming from memory, per `AGENTS.md`'s warning that this Next.js/package setup may differ from training data).
- `lib/liveblocks.ts` exports a `getLiveblocksClient()` function instead of an eagerly-constructed module-level client (unlike `lib/prisma.ts`'s eager singleton). `@liveblocks/node`'s `Liveblocks` constructor synchronously validates the secret key format and throws if it doesn't start with `sk_`; Next.js's build step evaluates route modules during "Collecting page data" even for a POST-only handler, so an eager `new Liveblocks(...)` at module scope failed `npm run build` outright whenever a real key isn't set yet (confirmed by reproducing the failure with the current empty placeholder key). Deferring construction until the handler actually runs sidesteps this; the `globalThis` caching itself still mirrors `lib/prisma.ts`'s pattern.
- `11-base-canvas.md`'s "error fallback for Liveblocks connection issues" is implemented with `useErrorListener` (via `CanvasConnectionGuard` in `components/editor/canvas-room.tsx`), not a React `ErrorBoundary` around `ClientSideSuspense` — the two are not interchangeable here. Liveblocks' auth-retry state machine only calls `fireErrorEvent` (which `useErrorListener` receives) for a fixed set of non-retryable HTTP status codes (`400/401/403/404/405/410/...`); everything else, including a `500`, is treated as transient and retried with backoff forever, without ever throwing into the render tree. A plain `ErrorBoundary` therefore only protects against genuine synchronous render throws (kept as `CanvasErrorBoundary`, still wrapping `ClientSideSuspense` as defense-in-depth), while `useErrorListener` is what actually surfaces the realistic "user lost access to this room" (`403`) case. Confirmed by reading `@liveblocks/core`'s compiled source and by direct testing (a `403` triggers the fallback; a `500`, correctly, keeps retrying/loading).
- `12-shape-panel.md`'s drag-and-drop node creation dispatches through `useLiveblocksFlow`'s own `onNodesChange` with a React-Flow-native `{ type: "add", item }` change, rather than any Liveblocks-specific "add node" call — `@liveblocks/react-flow` ships a server-only `mutateFlow`/`flow.addNode()` API (`@liveblocks/react-flow/node`, for webhook/backend use), but the client-side hook's `onNodesChange` is the only mutation surface exposed to a React component, and it recognizes the same `NodeChange` union (`add`/`position`/`select`/`remove`/`replace`/`dimensions`) that `@xyflow/react`'s own `applyNodeChanges` does — confirmed by reading the compiled source, not assumed from training data, per `AGENTS.md`.
- `components/editor/canvas.tsx` now wraps its React Flow instance in an explicit `<ReactFlowProvider>` (a `Canvas` → `CanvasInner` split), which wasn't needed in `11` since nothing used `useReactFlow()` yet. `useReactFlow()` (needed for `screenToFlowPosition`, the documented way to convert a drop event's `clientX`/`clientY` into canvas coordinates) only works in a descendant of `<ReactFlow>`'s own internally-rendered provider — calling it in the same component function that returns `<ReactFlow>...</ReactFlow>` throws, since that provider doesn't exist yet during that component's own render.
- `08-editor-workspace-shell.md` renamed the workspace route's dynamic segment from `[projectId]` to `[roomId]` — cosmetic/naming only, still the same Prisma `id` (see the `07` decision above on room-id = Prisma id). Rather than build a second, page-specific navbar/sidebar/right-panel shell for `/editor/[roomId]`, the existing shared `EditorShell` (mounted once in `app/editor/layout.tsx` for every `/editor/**` route) was made room-aware via `useParams` — it now conditionally shows the project name/Share/AI-toggle navbar actions and the right AI placeholder panel only when a `roomId` is present in the URL, leaving `/editor` home's chrome unchanged. This avoids double-mounting a navbar or sidebar, at the cost of `EditorShell` needing to know about room-specific UI it otherwise wouldn't.

## Session Notes

- shadcn CLI (v4.13.0) uses a new preset system; installed with the "Nova" preset (`Lucide / Geist`), which matches the project's existing icon/font choices, and the `radix` base library (Radix UI primitives via the unified `radix-ui` package) rather than the newer non-Radix `base` option.
- Do not modify `components/ui/*` — per `context/ai-workflow-rules.md`, project-specific styling belongs in app-level components instead.
- Next.js app router treats any `app/` folder prefixed with `_` as a private, non-routable segment — a temporary smoke-test route must use a plain folder name (e.g. `app/smoke-test/`) or it 404s.
- Gotcha: since `--color-base` is defined as a theme token in `globals.css`, Tailwind v4 auto-generates `bg-base` *and* `text-base` as color utilities — meaning `text-base` here is a text **color** (page background color), not Tailwind's default font-size utility. Don't use `text-base` for sizing; it silently collides with any text-color utility on the same element.
- Now that Clerk auth guards every route via `proxy.ts`, smoke-testing anything under `/editor` needs a temporary bypass: add a throwaway route (e.g. `app/smoke-test-04/`) and temporarily add its matcher (e.g. `"/smoke-test-04(.*)"`) to the `isPublicRoute` list in `proxy.ts`, then revert both after testing. Confirm the revert with `git status`/`git diff proxy.ts` before finishing.
- Gotcha: with Clerk dev-instance keys, direct `curl`/HTTP-client requests to any route behind `proxy.ts` (including `/api/*`) get a `307` redirect to `/sign-in` with header `x-clerk-auth-reason: dev-browser-missing`, regardless of method or real auth state — Clerk requires a browser-established `__clerk_db_jwt` handshake (set client-side by `clerk-js` on first page load) before it will even evaluate signed-in/signed-out status. There's no way to complete this handshake from curl. To exercise real 401/signed-out API behavior, use a headless browser (already carries the cookie from loading any page first) rather than curl.
- Gotcha: `npx playwright` doesn't add a `playwright` package to this project's `node_modules` — it runs from npm's own npx cache (`~/AppData/Local/npm-cache/_npx/<hash>/node_modules/playwright`). A standalone `node script.mjs` that does `import { chromium } from "playwright"` only resolves if the script is colocated in that same cache directory (find it via the cache path, `cp` the script there, then run `node script.mjs` from that directory).
- To smoke-test a feature that needs a *real* authenticated session (not just an unauthenticated redirect check), create a Clerk dev-instance test user via the Backend API using `CLERK_SECRET_KEY` from `.env.local` (`POST https://api.clerk.com/v1/users` with an `+clerk_test@` email and `skip_password_checks: true`), then drive the actual `/sign-in` UI with Playwright. Two OTP-style prompts can appear, both accept the fixed dev-mode code `424242`: the normal email-code step (if password sign-in isn't used/available), and — even after a successful password sign-in — a separate "check your device"/`client-trust` step that Clerk inserts for a browser it hasn't seen before (Playwright's context always looks new). Wait for navigation to settle after filling that code before asserting on the destination URL; it does a couple of internal redirects (`/sign-in/client-trust` → `/` → the app's real post-auth redirect target). Delete the test user via `DELETE /v1/users/<id>` on the same Backend API when done — it isn't cleaned up automatically. The Clerk CLI itself (`npx clerk@latest`) requires an interactive browser login (`clerk auth login`) and cannot authenticate non-interactively in this environment, so it's not viable for this — go straight to the Backend API with the app's existing secret key instead. For a two-user scenario (e.g. owner vs. collaborator), use two separate `browser.newContext()`s in the same Playwright script rather than signing in/out serially in one context — avoids cookie/session cleanup between the two and lets both be logged in "simultaneously" for cross-checks.
- Gotcha (confirmed as a real, user-reported bug, not just theoretical — and it recurred a second time): `--color-base` being a `@theme` color token means Tailwind v4 repurposes `text-base`/`bg-base` as *that color* (near-black, `#080809`) instead of the framework-default font-size meaning. shadcn's generated `components/ui/input.tsx`, `textarea.tsx`, and `dialog.tsx` (`DialogTitle`) all shipped with a stray `text-base` class from the original templates (meant as font-sizing). `tailwind-merge` (used in `lib/utils.ts`'s `cn()`) doesn't know about this project's custom color tokens, so it doesn't dedupe `text-base` against an explicit `text-copy-primary` override placed later in the same class string — both classes end up in the DOM, and plain CSS cascade (source order in the generated stylesheet, not class-attribute order) decided the color, which happened to make the text render almost invisible against the dark background. Fixed in all three files (`input.tsx`/`textarea.tsx` during `09`'s initial build, `dialog.tsx` during the same feature's later visual-redesign pass). `components/ui/card.tsx` has the identical `text-base` pattern (line ~41, card title) and has **not** been checked/fixed — it just hasn't been hit yet since `Card` isn't used in an app-level component with an explicit color override the way dialogs/inputs are. If `Card` gets used for something with dark-on-dark text, check this first. General rule going forward: check computed text color, not just presence/absence of a color-utility class in the source, when verifying dark-theme text legibility on any `components/ui/*` primitive that has a bare `text-base` in its default className.
- Gotcha: Clerk's post-sign-in `client-trust` step (the "check your device" OTP screen inserted for a browser Clerk hasn't seen, see the `07` session note on the two-OTP flow) can leave Playwright stuck on a blank `/` page indefinitely after the code is submitted — the client-side redirect chain (`/sign-in/client-trust` → `/` → `/editor`) is flaky under automation in this dev environment even though the server-side `redirect()` logic itself is correct (confirmed: `next dev`'s server log shows the correct `GET /editor 200` happening, the browser just never visibly completes the client-side hop). Workaround: after filling the OTP code, wait a few seconds for the session cookie to be set, then `page.goto()` the target route directly instead of waiting on the client router; a direct navigation works immediately since auth is already established. Also note the client-trust code input has no `name` attribute — select it with `input[autocomplete="one-time-code"]`, and use `locator(...).pressSequentially(...)` rather than `.fill()`, since Clerk's auto-submit-on-complete listener appears to key off real keystroke events rather than a value being set directly.
- Gotcha: testing real HTML5 drag-and-drop (native `draggable` + `dataTransfer`-based `dragstart`/`dragover`/`drop`, as used by `components/editor/shape-panel.tsx` → `canvas.tsx`) with Playwright cannot use the built-in `locator.dragTo()` helper — that simulates mouse movement only and never populates a real `DataTransfer`, so handlers reading `event.dataTransfer.getData(...)` see nothing. Instead, use `page.evaluate()` to construct a real `DataTransfer`, dispatch a `dragstart` `DragEvent` (carrying that `DataTransfer`) on the drag source element, then dispatch `dragover` and `drop` `DragEvent`s (same `DataTransfer`, at the target `clientX`/`clientY`) on `document.elementFromPoint(x, y)` at the drop target. Worked on the first attempt when testing `12-shape-panel.md`.
- Gotcha: `eslint-config-next`'s React Compiler rules include `react-hooks/set-state-in-effect`, which errors (not warns) on *any* direct `setState` call in a `useEffect` body — including the extremely common "set a loading flag, then fetch" pattern. There's no targeted escape hatch that keeps the pattern; the fix used here (`hooks/use-share-dialog.ts`) was to not use an effect at all for "fetch when a dialog opens" — instead expose a plain async `load()` function and call it directly from the real click handler that flips the dialog's `open` state (in `EditorShell`, one level up), since that handler is a genuine user-event callback rather than an effect. Worth checking for this pattern before reaching for `useEffect` + `fetch` on any future on-demand-loaded UI (dialogs, popovers, tabs).
